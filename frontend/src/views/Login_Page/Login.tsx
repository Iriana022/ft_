import {LoginCard} from './LoginCard';

export default function Login() {
	return (
		<div className="min-h-screen w-full flex items-center justify-center p-5 transition-colors duration-300">
			<LoginCard />
		</div>
	);
}
